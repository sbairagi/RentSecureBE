# =============================================================================
# RentSecureBE – Production Runtime Dependencies (Phase 4A – Approved)
# =============================================================================
# Generated: 2026-05-06
# Approved by: Principal Staff Engineer / SaaS Architect
# Strategy: KEEP NOW (22) + UPGRADED 1 + ADDED 4 + (razorpay upgrade DEFERRED)
# Total production top-level: 27 packages
# Reduction from 217 → 27 (-88%)
# =============================================================================

# -----------------------------------------------------------------------------
# --- A. KEEP NOW: Currently required by production code (22 packages) ---
# -----------------------------------------------------------------------------

# --- Django core (LTS) ---
Django==4.2.30
asgiref==3.8.1

# --- Build / packaging (razorpay 1.4.2 requires pkg_resources) ---
setuptools<70                       # pin below 70 to keep pkg_resources importable

# --- REST API + JWT Auth ---
djangorestframework==3.16.0
djangorestframework_simplejwt==5.5.0

# --- Audit / History tracking ---
django-simple-history==3.8.0

# --- Configuration / Environment ---
python-decouple==3.5

# --- Date / Time helpers ---
python-dateutil==2.9.0.post0

# --- HTTP / Networking ---
requests==2.32.3

# --- PDF Generation & Merging ---
weasyprint==66.0

# --- Spreadsheet / Excel exports ---
openpyxl==3.1.5
xlsxwriter==3.2.9

# --- Twilio: SMS / WhatsApp / Voice ---
twilio==9.6.1

# --- Voice notes via gTTS ---
gTTS==2.5.4

# --- Firebase Cloud Messaging ---
fcm-django==2.2.1
firebase-admin==6.8.0

# --- Translations ---
deep-translator==1.11.4

# --- Background tasks / Celery (infrastructure) ---
celery==5.6.3
django-celery-beat==2.9.0

# --- Razorpay Payments (no upgrade possible in 1.4.x — current 1.4.2 IS the latest 1.4.x) ---
razorpay==1.4.2

# -----------------------------------------------------------------------------
# --- B. UPGRADED (1) ---
# -----------------------------------------------------------------------------

# --- AWS SDK + S3 (active production use in whatsapp_service.py) ---
boto3==1.35.28                       # was 1.18.53; CVE + urllib3 2.x upgrade

# -----------------------------------------------------------------------------
# --- C. ADDED (4 critical missing production deps) ---
# -----------------------------------------------------------------------------

# --- AI Assistant (OpenAI v0.x — code uses legacy API) ---
openai==0.28.1                      # smartbot/services/{chatbot_service,gpt_services}.py
                                     # NOTE: 0.28.x required because code uses openai.ChatCompletion.create
                                     #       and openai.api_key = ... APIs which were removed in 1.0+

# --- Image handling (ImageField backend) ---
Pillow==11.2.1                      # properties/models/{caretaker,renter,unit}_models.py

# --- PDF Merging ---
PyPDF2==3.0.1                       # documents/utils.py: from PyPDF2 import PdfMerger

# --- PostgreSQL production driver ---
psycopg2-binary==2.9.10             # settings.DATABASES supports PG; CI installs separately

# =============================================================================
# END — Total: 27 production top-level packages
# =============================================================================
# REMOVED in Phase 4A (was in lockfile, 0 evidence of use):
#   Flask==3.1.1, fpdf==1.7.2, fpdf2==2.8.3, selenium==4.29.0, SQLAlchemy==2.0.41,
#   pandas==2.2.2, PyMySQL==1.0.2, instagram-private-api==1.6.0.0, instaloader==4.14.1,
#   django-crum==0.7.9, django-multiselectfield==0.1.12, django-otp==1.6.0,
#   django-rest-auth==0.9.5, django-s3-storage==0.13.4, django-select2==8.2.3,
#   django-widget-tweaks==1.4.8, drf-writable-nested==0.7.0,
#   google-api-python-client==2.170.0, google-cloud-core==2.4.3, arrow==1.3.0
#
# KEPT FOR ROADMAP (NOT modified in this PR — needs code + architect decision):
#   django-cors-headers==3.9.0, whitenoise==5.3.0, django-storages==1.11.1,
#   dj-database-url==2.3.0
#
# razorpay upgrade DEFERRED: only 1.4.1, 1.4.2, 2.0.0, 2.0.1 exist on PyPI.
# 1.4.2 is already the latest 1.4.x release. 2.0.x has breaking changes
# (Payment entity renamed to Order) and requires code migration.
# =============================================================================
