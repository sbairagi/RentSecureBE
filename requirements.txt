# =============================================================================
# RentSecureBE – Unified Dependencies (Runtime + Dev)
# =============================================================================
# Source: requirements.txt (runtime) + requirements-dev.txt (dev/CI)
# Created: 2026-06-22
# Python : 3.12
# Django: 4.2.30
# DRF    : 3.16.x
#
# This is the SINGLE authoritative dependency file.
# Install with: pip install -r requirements.txt
# =============================================================================
# -----------------------------------------------------------------------------
# A. RUNTIME / PRODUCTION DEPENDENCIES
# -----------------------------------------------------------------------------
# Django core (LTS)
Django==4.2.30
django-extensions==4.1
asgiref==3.8.1
# Build / packaging (razorpay 1.4.2 requires pkg_resources)
setuptools==78.1.1
# REST API + JWT Auth
djangorestframework==3.16.0
djangorestframework_simplejwt==5.5.1
# Audit / History tracking
django-simple-history==3.8.0
# Configuration / Environment
python-decouple==3.5
# Date / Time helpers
python-dateutil==2.9.0.post0
# HTTP / Networking
requests==2.33.0
# PDF Generation & Merging
weasyprint==69.0
PyPDF2==3.0.1
# Image handling (ImageField backend)
Pillow==12.2.0
# Spreadsheet / Excel exports
openpyxl==3.1.5
xlsxwriter==3.2.9
# Twilio: SMS / WhatsApp / Voice
twilio==9.6.1
# Voice notes via gTTS
gTTS==2.5.4
# Firebase Cloud Messaging
fcm-django==2.2.1
firebase-admin==6.8.0
# Translations
deep-translator==1.11.4
# Background tasks / Celery (infrastructure)
celery==5.6.3
django-celery-beat==2.9.0
# Razorpay Payments
razorpay==1.4.2
# AWS SDK + S3
boto3==1.35.28
# AI Assistant (OpenAI v0.x — code uses legacy API)
openai==0.28.1
# PostgreSQL production driver
psycopg2-binary==2.9.10
# -----------------------------------------------------------------------------
# B. DEVELOPMENT / CI / TEST DEPENDENCIES
# -----------------------------------------------------------------------------
# Type Checking
mypy==1.13.0
django-stubs==6.0.5
django-stubs-ext==6.0.5
djangorestframework-stubs==3.17.0
types-requests==2.33.0.20260518
types-python-dateutil==2.9.0.20260518
types-openpyxl==3.1.5.20260518
# Linting & Formatting
black==26.3.1
ruff==0.15.14
pylint==4.0.6
pylint-django==2.7.0
# Security Scanning
bandit==1.8.3
pip-audit==2.7.3
# Testing Framework
pytest==9.0.3
pytest-django==4.11.1
pytest-cov==7.0.0
pytest-randomly==3.15.0
pytest-split==0.11.0
pytest-benchmark==4.0.0
coverage[toml]==7.10.6
factory-boy==3.3.0
Faker==30.8.0
hypothesis==6.136.0
# Code Quality
vulture==2.14
import-linter==1.6.0
mutmut==3.6.0
isort==5.13.2
autoflake==2.2.1
pre-commit==4.2.0
# Schema / Contract Testing
schemathesis==4.21.10
# Load Testing
locust==2.32.0
# Database URL parsing (roadmap)
dj-database-url==2.3.0
# Django Channels + Redis
channels==4.2.0
channels-redis==4.1.0
# =============================================================================
# END — 56 pinned packages (27 runtime + 29 dev/test/stubs)
# =============================================================================
#
# Migration notes:
#   - requirements.txt  (this file) is now the unified authoritative source.
#   - requirements-dev.txt, requirements_proposed_final.txt,
#     requirements_dev_proposed_final.txt remain as READ-ONLY archives.
#   - gitleaks and semgrep were never pip dependencies; removed.
#     (semgrep/semgrep-action@v1, gitleaks/gitleaks-action@v2 are GitHub Actions.)
#   - type stubs updated to venv-resolved versions.
#   - pylint resolved to 3.3.2 (pylint-django requires <4).
#   - pytest resolved to 9.0.3 (CVE-2025-71176 fix; schemathesis bumped to 4.21.10 for compat).
#   - coverage[toml] resolved to 7.10.6 (pytest-cov requires >=7.10.6).
#   - import-linter resolved to 1.6.0 (1.6.6 never published on PyPI).
#   - mutmut resolved to 3.6.0 (4.2.0 never published on PyPI).
# =============================================================================
    # pip-audit: subdependency explicitly fixed
starlette==1.3.1
