# RentSecureBE — Phase 0 Execution Plan

**Phase:** Phase 0: Foundation & Critical Fixes
**Duration:** Week 2-3
**Status:** READY FOR EXECUTION
**Prerequisites:** Phase -1 complete (type_compat moved, cashfree_payout moved, payments registered, circular dependencies broken)
**Constraint:** No breaking changes. All changes additive or internal refactors.

---

## Phase 0 Overview

| Metric | Value |
|--------|-------|
| **Total PRs** | 9 |
| **Total Tasks** | 15 |
| **Estimated Effort** | 80 hours |
| **Risk Level** | Medium |
| **Breaking Changes** | None |
| **Rollback Required** | Per PR |

### PR Dependency Graph

```
PR-001 (Foundation) ──┬──→ PR-002 (OwnerBankDetails migration)
                      ├──→ PR-003 (NotificationPreference migration)
                      ├──→ PR-004 (Webhooks)
                      ├──→ PR-005 (Core views split)
                      ├──→ PR-006 (Management commands)
                      └──→ PR-007 (Import-linter + arch tests)
                                 │
                                 ├──→ PR-008 (Idempotency + audit)
                                 │
                                 └──→ PR-009 (Migration tests + CI)
```

---

# PR-001: Foundation — Encrypted Fields & Payments Registration

## Goal

Establish the foundational pieces for Phase 0: create encrypted field types, register the `payments` app, and create the initial `payments/models.py` with `OwnerBankDetails`. This PR must land first because all subsequent Phase 0 PRs depend on `payments` being a registered Django app.

## Files to Create

| File | Purpose |
|------|---------|
| `shared/fields.py` | Encrypted field types (`EncryptedCharField`, `EncryptedTextField`) using `django-cryptography` |
| `payments/migrations/__init__.py` | Mark `payments/migrations/` as a Python package |
| `payments/migrations/0001_initial.py` | Initial empty migration for `payments` app (generated by `makemigrations`) |
| `payments/models.py` | `OwnerBankDetails` model with encrypted `bank_account_number` and `ifsc_code` fields |
| `shared/tests/test_fields.py` | Unit tests for encrypted field types |
| `payments/tests/__init__.py` | Test package marker |
| `payments/tests/test_models.py` | Model tests for `OwnerBankDetails` |

## Files to Modify

| File | Change |
|------|--------|
| `rentsecure_be/settings.py` | Add `"payments"` to `INSTALLED_APPS` |
| `pyproject.toml` | Add `django-cryptography` to dependencies (if not already present) |
| `requirements.txt` | Add `django-cryptography` (if not already present) |

## Files to Delete

None.

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all existing tests pass, new tests pass |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — no violations |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| `payments/` in `INSTALLED_APPS` | ✅ Confirmed |
| Encrypted fields used for sensitive data | ✅ `bank_account_number` and `ifsc_code` use `EncryptedCharField` |
| No `AUTH_USER_MODEL` change | ✅ `AUTH_USER_MODEL` remains `"core.User"` |
| No circular dependencies | ✅ AST analysis confirms 0 cycles |
| No `rentsecure_be/` imports in apps | ✅ AST scan confirms 0 violations (Phase -1 already complete) |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |
| `payments/` layer allowed imports | ✅ `payments/` can import from `shared/`, `platform/`, `identity/` |
| No app imports from `rentsecure_be/` | ✅ Confirmed (Phase -1) |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |
| `shared/fields.py` type annotations | ✅ Complete |
| `payments/models.py` type annotations | ✅ Complete |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |
| Import sorting | ✅ Correct |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| Existing unit tests | ✅ All pass — no regressions |
| Existing integration tests | ✅ All pass — no regressions |
| `shared/tests/test_fields.py` | ✅ Pass — encrypted fields encrypt/decrypt correctly |
| `payments/tests/test_models.py` | ✅ Pass — `OwnerBankDetails` CRUD operations work |
| Migration tests | ✅ Pass — `makemigrations` generates expected migration, `migrate` applies cleanly |
| `manage.py check` | ✅ Pass — no system check errors |

## Rollback Plan

**Rollback Trigger:** Any CI gate fails, data migration corrupts data, or production issue detected.

**Rollback Steps:**
1. `git revert` PR-001 merge commit
2. `python manage.py migrate --reverse` to remove `payments` initial migration
3. Remove `"payments"` from `INSTALLED_APPS` (if added in this PR)
4. Deploy reverted code
5. Run smoke tests
6. Verify production health

**Rollback Time:** 15 minutes
**Data Risk:** None — initial migration is empty or adds new tables only
**Rollback Testing:** Dry-run on staging before production deploy

## Definition of Done

- [ ] `shared/fields.py` created with `EncryptedCharField` and `EncryptedTextField`
- [ ] `payments/models.py` created with `OwnerBankDetails` using encrypted fields
- [ ] `"payments"` added to `INSTALLED_APPS`
- [ ] `payments/migrations/0001_initial.py` generated and applied
- [ ] All new tests pass
- [ ] All existing tests pass
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] `manage.py check` passes
- [ ] PR approved by Platform Team Lead and Security Lead
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)

---

# PR-002: Migrate OwnerBankDetails to payments

## Goal

Move `OwnerBankDetails` from `core/models.py` to `payments/models.py` with a data migration that preserves all existing data. The old `core_ownerbankdetails` table remains for one release cycle as a deprecated shim.

## Files to Create

| File | Purpose |
|------|---------|
| `payments/migrations/0002_migrate_ownerbankdetails.py` | Data migration: copies `core_ownerbankdetails` → `payment_ownerbankdetails` |
| `core/migrations/XXXX_migrate_ownerbankdetails.py` | Data migration: copies `core_ownerbankdetails` → `payment_ownerbankdetails` (source table) |
| `payments/tests/test_migrations.py` | Migration tests: forward and reverse |
| `payments/tests/test_bank_details_service.py` | Service tests for bank details (if service exists) |

## Files to Modify

| File | Change |
|------|--------|
| `payments/models.py` | Ensure `OwnerBankDetails` matches original fields exactly |
| `core/models.py` | Add deprecation warning to `OwnerBankDetails` or remove (keep shim for 1 release cycle) |
| `core/views/bank_views.py` | Update to use `payments/models.py` `OwnerBankDetails` (delegation) |
| `core/services/bank_details_service.py` | Update to use `payments/models.py` `OwnerBankDetails` (delegation) |

## Files to Delete

None (old `core_ownerbankdetails` table remains in database; model may be deprecated but not deleted in this PR).

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all tests pass including migration tests |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — no violations |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| `OwnerBankDetails` in `payments/models.py` | ✅ Confirmed |
| Encrypted fields used | ✅ `bank_account_number` and `ifsc_code` use `EncryptedCharField` |
| No `OwnerBankDetails` in `core/models.py` (or deprecated) | ✅ Confirmed |
| Data migration preserves all rows | ✅ Row count matches before/after |
| No circular dependencies | ✅ Confirmed |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |
| No `core.models.OwnerBankDetails` imports in `payments/` | ✅ `payments/` uses its own model |
| No `payments.models.OwnerBankDetails` imports in `core/` | ✅ `core/` delegates to `payments/` |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |
| Type compatibility between old and new model | ✅ Verified |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| Existing tests | ✅ All pass — no regressions |
| Migration forward test | ✅ `migrate` copies all rows from `core_ownerbankdetails` to `payment_ownerbankdetails` |
| Migration reverse test | ✅ `migrate --reverse` restores original state |
| Data integrity test | ✅ Row count, checksums, and field values match |
| `OwnerBankDetails` model tests | ✅ CRUD operations work with encrypted fields |
| Bank details API tests | ✅ API endpoints work with new model location |

## Rollback Plan

**Rollback Trigger:** Data migration fails, data corruption detected, or tests fail after merge.

**Rollback Steps:**
1. `git revert` PR-002 merge commit
2. `python manage.py migrate --reverse` to undo data migration
3. Restore `core/models.py` `OwnerBankDetails` if removed
4. Deploy reverted code
5. Run smoke tests
6. Verify `core_ownerbankdetails` table is accessible

**Rollback Time:** 30 minutes
**Data Risk:** Low — old table remains. Data migration is additive (copies data, doesn't delete).
**Rollback Testing:** Tested on staging with production-like data before merge.

## Definition of Done

- [ ] `payments/migrations/0002_migrate_ownerbankdetails.py` created and tested
- [ ] Data migration copies all rows from `core_ownerbankdetails` to `payment_ownerbankdetails`
- [ ] `core/models.py` `OwnerBankDetails` deprecated with `DeprecationWarning`
- [ ] All cross-app imports updated to use `payments.models.OwnerBankDetails`
- [ ] Migration forward test passes
- [ ] Migration reverse test passes
- [ ] Data integrity verified (row counts, checksums)
- [ ] All existing tests pass
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] PR approved by Platform Team Lead and Security Lead
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)

---

# PR-003: Migrate NotificationPreference to notification

## Goal

Move `NotificationPreference` from `core/models.py` to `notification/models.py` with a data migration. The old `core_notificationpreference` table remains for one release cycle.

## Files to Create

| File | Purpose |
|------|---------|
| `notification/models.py` | `NotificationPreference` model (moved from `core/models.py`) |
| `notification/migrations/0001_initial.py` | Initial migration for `notification` app with `NotificationPreference` |
| `core/migrations/XXXX_migrate_notificationpreference.py` | Data migration: copies `core_notificationpreference` → `notification_notificationpreference` |
| `notification/tests/test_models.py` | Model tests for `NotificationPreference` |
| `notification/tests/test_migrations.py` | Migration forward/reverse tests |

## Files to Modify

| File | Change |
|------|--------|
| `core/models.py` | Remove or deprecate `NotificationPreference` with `DeprecationWarning` |
| `core/signals.py` | Update to create `NotificationPreference` via `notification/` service (or keep using `core/` shim during transition) |
| `identity/signals.py` | Update to create `NotificationPreference` via `notification/` service (Phase 1, but prepare in Phase 0) |

## Files to Delete

None (old `core_notificationpreference` table remains in database).

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all tests pass including migration tests |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — no violations |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| `NotificationPreference` in `notification/models.py` | ✅ Confirmed |
| No `NotificationPreference` in `core/models.py` (or deprecated) | ✅ Confirmed |
| Data migration preserves all rows | ✅ Row count matches |
| No circular dependencies | ✅ Confirmed |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |
| No `core.models.NotificationPreference` imports in `notification/` | ✅ `notification/` uses its own model |
| No `notification.models.NotificationPreference` imports in `core/` | ✅ `core/` delegates or uses shim |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| Existing tests | ✅ All pass — no regressions |
| Migration forward test | ✅ All rows copied |
| Migration reverse test | ✅ Original state restored |
| Data integrity test | ✅ Row counts and values match |
| `NotificationPreference` model tests | ✅ CRUD operations work |
| Notification preference API tests | ✅ API endpoints work |

## Rollback Plan

**Rollback Trigger:** Data migration fails, data corruption, or tests fail.

**Rollback Steps:**
1. `git revert` PR-003 merge commit
2. `python manage.py migrate --reverse` to undo data migration
3. Restore `core/models.py` `NotificationPreference` if removed
4. Deploy reverted code
5. Run smoke tests

**Rollback Time:** 30 minutes
**Data Risk:** Low — old table remains.
**Rollback Testing:** Tested on staging with production-like data.

## Definition of Done

- [ ] `notification/models.py` created with `NotificationPreference`
- [ ] `notification/migrations/0001_initial.py` generated and tested
- [ ] Data migration copies all rows from `core_notificationpreference` to `notification_notificationpreference`
- [ ] `core/models.py` `NotificationPreference` deprecated with `DeprecationWarning`
- [ ] All cross-app imports updated
- [ ] Migration forward test passes
- [ ] Migration reverse test passes
- [ ] Data integrity verified
- [ ] All existing tests pass
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] PR approved by Platform Team Lead
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)

---

# PR-004: Move Webhooks to payments

## Goal

Move Cashfree and Razorpay webhook handlers from `core/views.py` to `payments/views/webhooks.py`, add webhook URLs to `payments/urls.py`, and keep old URLs in `core/urls.py` as deprecated 301/302 redirects. This PR must include webhook idempotency keys (finding 0.9).

## Files to Create

| File | Purpose |
|------|---------|
| `payments/views/webhooks.py` | `cashfree_payout_webhook` and `razorpay_webhook` views with HMAC verification and idempotency |
| `payments/views/__init__.py` | Package marker |
| `payments/urls.py` | Webhook URL patterns |
| `payments/tests/test_webhooks.py` | Unit and integration tests for webhook handlers |
| `payments/tests/test_webhook_idempotency.py` | Idempotency tests (duplicate webhooks) |
| `payments/tests/test_webhook_security.py` | Security tests (invalid signatures rejected) |

## Files to Modify

| File | Change |
|------|--------|
| `core/views.py` | Remove `cashfree_payout_webhook` and `razorpay_webhook` function-based views |
| `core/views/bank_views.py` | No change needed (webhooks separate from bank views) |
| `core/urls.py` | Replace webhook handlers with 301/302 redirects to new `payments/` URLs |
| `rentsecure_be/urls.py` | Add `path("api/webhook/", include("payments.urls"))` BEFORE `core/` URLs |
| `payments/models.py` | Add `WebhookEvent` model with `event_id` unique constraint |
| `payments/migrations/0002_webhook_event.py` | Migration for `WebhookEvent` model |

## Files to Delete

None (old webhook handlers in `core/views.py` are removed, but old URLs remain as redirects).

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all tests pass including webhook tests |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — no violations |
| Security | ✅ Pass — Bandit 0 high/medium |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| Webhook handlers in `payments/views/webhooks.py` | ✅ Confirmed |
| No webhook handlers in `core/views.py` | ✅ Confirmed |
| No `razorpay` SDK imports in `core/` | ✅ Confirmed |
| No `cashfree` SDK imports in `core/` | ✅ Confirmed |
| Webhook idempotency implemented | ✅ `WebhookEvent` model prevents duplicate processing |
| HMAC verification in adapter layer | ✅ Confirmed in `payments/views/webhooks.py` |
| Old URLs return 301/302 | ✅ Confirmed |
| No circular dependencies | ✅ Confirmed |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |
| No `razorpay` imports in `core/` | ✅ Confirmed |
| No `cashfree` imports in `core/` | ✅ Confirmed |
| `payments/` can import from `shared/`, `platform/`, `identity/` | ✅ Confirmed |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |
| `payments/views/webhooks.py` type annotations | ✅ Complete |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| Existing tests | ✅ All pass — no regressions |
| Webhook handler tests | ✅ Pass — Cashfree and Razorpay payloads handled correctly |
| Webhook idempotency tests | ✅ Pass — duplicate webhooks return 200 with original response |
| Webhook security tests | ✅ Pass — invalid signatures rejected with 401 |
| Redirect tests | ✅ Pass — old URLs return 301/302 to new URLs |
| Migration tests | ✅ Pass — `WebhookEvent` migration forward/reverse |

## Rollback Plan

**Rollback Trigger:** Webhook handler fails, provider callback fails, or idempotency logic causes data loss.

**Rollback Steps:**
1. `git revert` PR-004 merge commit
2. `python manage.py migrate --reverse` to remove `WebhookEvent` model
3. Restore `core/views.py` webhook handlers from git
4. Remove `payments/urls.py` include from `rentsecure_be/urls.py`
5. Remove redirects from `core/urls.py`
6. Deploy reverted code
7. Run smoke tests (send test webhooks from provider dashboards)
8. Verify old webhook URLs respond correctly

**Rollback Time:** 30 minutes
**Data Risk:** Low — `WebhookEvent` table can be removed. Old webhook handlers restored.
**Rollback Testing:** Tested on staging with provider test environments.

## Definition of Done

- [ ] `payments/views/webhooks.py` created with Cashfree and Razorpay handlers
- [ ] `payments/urls.py` created with webhook URL patterns
- [ ] `rentsecure_be/urls.py` includes `payments/urls.py`
- [ ] `core/urls.py` webhook URLs return 301/302 redirects
- [ ] `WebhookEvent` model created with `event_id` unique constraint
- [ ] Webhook idempotency implemented and tested
- [ ] HMAC verification implemented for both providers
- [ ] All webhook tests pass (unit, integration, security, idempotency)
- [ ] All existing tests pass
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] Bandit scan passes (0 high/medium)
- [ ] PR approved by Platform Team Lead and Security Lead
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (48 hours — webhooks require extended validation)
- [ ] Provider test webhooks verified (Cashfree test mode, Razorpay test mode)

---

# PR-005: Split core/views.py

## Goal

Split the 566-line `core/views.py` god view into 4 focused modules: `auth_views.py`, `subscription_views.py`, `bank_views.py`, and `reporting_views.py`. No functional changes — pure refactor.

## Files to Create

| File | Purpose |
|------|---------|
| `core/views/__init__.py` | Package marker |
| `core/views/auth_views.py` | OTP, authentication views (SendOTP, OwnerVerifyOTP, RenterVerifyOTP, etc.) |
| `core/views/subscription_views.py` | Subscription CRUD views |
| `core/views/bank_views.py` | Bank details views |
| `core/views/reporting_views.py` | Owner reporting views (rent inflow summary, rent records, Excel download) |

## Files to Modify

| File | Change |
|------|--------|
| `core/views.py` | Remove all view classes/functions (moved to submodules) or replace with imports from submodules |
| `rentsecure_be/urls.py` | Update imports from `core.views` to `core.views.<module>` |
| `core/urls.py` | Update imports from `core.views` to `core.views.<module>` |
| Any file importing from `core.views` | Update import paths |

## Files to Delete

| File | Reason |
|------|--------|
| `core/views.py` | Replaced by `core/views/` package |

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all view tests pass |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — no god view violations (max 300 lines per view file) |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| No view file exceeds 300 lines | ✅ Confirmed by `test_god_views.py` |
| No `core.views` imports (use submodules) | ✅ AST scan confirms |
| All URLs resolve correctly | ✅ URL resolution tests pass |
| No circular dependencies | ✅ Confirmed |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |
| No `from core.views import` (direct module imports) | ✅ All imports use `core.views.<module>` |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| Existing view tests | ✅ All pass — no functional changes |
| URL resolution tests | ✅ All URLs resolve correctly |
| Import tests | ✅ All imports work |
| God view detection test | ✅ No view file exceeds 300 lines |

## Rollback Plan

**Rollback Trigger:** View tests fail, URL resolution fails, or import errors occur.

**Rollback Steps:**
1. `git revert` PR-005 merge commit
2. Restore `core/views.py` from git
3. Revert imports in `rentsecure_be/urls.py` and `core/urls.py`
4. Deploy reverted code
5. Run smoke tests on all endpoints
6. Verify all views accessible

**Rollback Time:** 15 minutes
**Data Risk:** None — no data changes.
**Rollback Testing:** Tested on staging before merge.

## Definition of Done

- [ ] `core/views.py` split into 4 focused modules
- [ ] No view file exceeds 300 lines
- [ ] All view tests pass
- [ ] All URL resolution tests pass
- [ ] All imports updated across codebase
- [ ] `core/views.py` removed or replaced with package imports
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] PR approved by Staff Engineer
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)

---

# PR-006: Move Root Management Commands to Apps

## Goal

Move all 15 root-level management commands from `management/commands/` to their owning app's `management/commands/` directory. Remove duplicate commands.

## Files to Create

| File | Purpose |
|------|---------|
| `properties/management/commands/apply_late_fees.py` | Late fee calculation command (moved from root) |
| `properties/management/commands/auto_deactivate_renters.py` | Renter deactivation command (moved from root) |
| `properties/management/commands/check_vacant_units.py` | Vacant unit check command (moved from root) |
| `properties/management/commands/generate_monthly_rent_records.py` | Rent record generation command (moved from root) |
| `properties/management/commands/daily_rent_reminder.py` | Daily rent reminder command (moved from root) |
| `properties/management/commands/send_rent_reminders.py` | Rent reminder command (moved from root) |
| `notification/management/commands/send_extra_charge_reminders.py` | Extra charge reminder command (moved from root) |
| `notification/management/commands/rent_reminder_service.py` | Rent reminder service command (moved from root) |
| `notification/management/commands/send_rent_reminders.py` | Rent reminder command (moved from root) |
| `finance/management/commands/send_tax_reminders.py` | Tax reminder command (moved from root) |
| `finance/management/commands/retry_failed_payouts.py` | Payout retry command (moved from root) |
| `core/management/commands/monthly_whatsapp_and_email_summary_to_owner.py` | Owner summary command (moved from root) |
| `core/management/commands/downgrade_expired_users.py` | User downgrade command (moved from root) |
| `core/management/commands/archive_expired_users_data.py` | Archive command (moved from root) |
| `core/management/commands/seed_subscription_plans.py` | Seed command (moved from root) |
| `core/management/commands/send_monthly_rent_summary.py` | Monthly summary command (moved from root) |
| `core/management/commands/retry_failed_payouts.py` | Payout retry command (moved from root — duplicate) |

## Files to Modify

| File | Change |
|------|--------|
| `core/management/commands/` | Update to use moved commands or remove duplicates |
| `properties/management/commands/` | Update to use moved commands |
| `notification/management/commands/` | Update to use moved commands |
| `finance/management/commands/` | Update to use moved commands |

## Files to Delete

| File | Reason |
|------|--------|
| `management/commands/apply_late_fees.py` | Moved to `properties/management/commands/` |
| `management/commands/auto_deactivate_renters.py` | Moved to `properties/management/commands/` |
| `management/commands/check_vacant_units.py` | Moved to `properties/management/commands/` |
| `management/commands/generate_monthly_rent_records.py` | Moved to `properties/management/commands/` |
| `management/commands/daily_rent_reminder.py` | Moved to `properties/management/commands/` or `notification/` |
| `management/commands/send_rent_reminders.py` | Moved to `properties/` or `notification/` |
| `management/commands/send_monthly_rent_summary.py` | Moved to `properties/` (duplicate of existing) |
| `management/commands/send_extra_charge_reminders.py` | Moved to `notification/` |
| `management/commands/rent_reminder_service.py` | Moved to `notification/` |
| `management/commands/retry_failed_payouts.py` | Moved to `finance/` |
| `management/commands/send_tax_reminders.py` | Moved to `finance/` |
| `management/commands/monthly_whatsapp_and_email_summary_to_owner.py` | Removed (duplicate of `send_monthly_rent_summary`) |
| `management/commands/downgrade_expired_users.py` | Moved to `core/` |
| `management/commands/archive_expired_users_data.py` | Moved to `core/` |
| `management/commands/seed_subscription_plans.py` | Moved to `core/` |

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all tests pass |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — no root management commands |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| No management commands at project root | ✅ Confirmed by architecture test |
| Commands in app `management/` directories | ✅ Confirmed |
| No duplicate commands | ✅ Confirmed |
| No circular dependencies | ✅ Confirmed |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |
| No imports from root `management.commands` | ✅ Confirmed |
| Commands importable via `python manage.py <command>` | ✅ Confirmed |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| Existing tests | ✅ All pass — no regressions |
| Command execution tests | ✅ Each command runs successfully via `call_command` |
| Command output tests | ✅ Output matches expected |
| Integration tests | ✅ Scheduled tasks work correctly |

## Rollback Plan

**Rollback Trigger:** Command fails, import errors, or tests fail.

**Rollback Steps:**
1. `git revert` PR-006 merge commit
2. Restore all root `management/commands/` files from git
3. Remove app-level `management/commands/` directories (or keep as duplicates)
4. Deploy reverted code
5. Verify commands run correctly via `python manage.py <command>`

**Rollback Time:** 30 minutes
**Data Risk:** None — management commands do not modify schema.
**Rollback Testing:** Tested on staging — all commands verified to run.

## Definition of Done

- [ ] All 15 root management commands moved to app `management/commands/` directories
- [ ] Duplicate commands removed
- [ ] Each command is importable via `python manage.py <command>`
- [ ] No management commands remain at project root
- [ ] All command tests pass
- [ ] All existing tests pass
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] PR approved by Product Team Lead and Platform Team Lead
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)

---

# PR-007: Rewrite import-linter.ini + Add Architecture Regression Tests

## Goal

Rewrite `import-linter.ini` to remove `rentsecure_be` as an allowed layer for all apps, and add comprehensive architecture regression tests that enforce all v1.1 architectural rules on every commit.

## Files to Create

| File | Purpose |
|------|---------|
| `tests/architecture/__init__.py` | Package marker |
| `tests/architecture/test_import_rules.py` | Tests for SDK placement, `rentsecure_be/` boundary, `Notification.objects.create` rule |
| `tests/architecture/test_layer_compliance.py` | Tests for view-model and view-service import boundaries |
| `tests/architecture/test_sdk_placement.py` | Tests ensuring payment SDKs only in adapter modules |
| `tests/architecture/test_god_views.py` | Test ensuring no view file exceeds 300 lines |
| `tests/architecture/test_god_models.py` | Test ensuring no model file exceeds 400 lines |
| `tests/architecture/test_circular_deps.py` | Test ensuring no circular dependencies |
| `tests/architecture/test_rentsecure_be_boundary.py` | Test ensuring no app imports from `rentsecure_be/` |
| `tests/architecture/test_shared_purity.py` | Test ensuring `shared/` does not import Django or apps |

## Files to Modify

| File | Change |
|------|--------|
| `import-linter.ini` | Complete rewrite: remove `rentsecure_be` as allowed layer, add all apps per v1.1 matrix |
| `.github/workflows/ci.yml` | Ensure architecture tests run on every commit |

## Files to Delete

None.

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all tests pass including architecture tests |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — all architecture tests pass |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| No `rentsecure_be/` imports in apps | ✅ `test_rentsecure_be_boundary.py` passes |
| No circular dependencies | ✅ `test_circular_deps.py` passes |
| No god views (>300 lines) | ✅ `test_god_views.py` passes |
| No god models (>400 lines) | ✅ `test_god_models.py` passes |
| No payment SDKs in non-adapter files | ✅ `test_sdk_placement.py` passes |
| No `Notification.objects.create` outside `notification/` | ✅ `test_import_rules.py` passes |
| `shared/` purity | ✅ `test_shared_purity.py` passes |
| View-layer compliance | ✅ `test_layer_compliance.py` passes |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |
| `payments/` layer | ✅ Can import from `shared/`, `platform/`, `identity/`, `property/` |
| `notification/` layer | ✅ Can import from `shared/`, `platform/`, `identity/` only |
| `property/` layer | ✅ Can import from `shared/`, `platform/`, `identity/`, `subscription/` |
| No app can import from `rentsecure_be/` | ✅ Confirmed |
| `shared/` layer | ✅ Cannot import from any app |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |
| Architecture test files | ✅ Type-checked |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| `test_import_rules.py` | ✅ Pass — catches known violations |
| `test_layer_compliance.py` | ✅ Pass — views do not import models from other apps |
| `test_sdk_placement.py` | ✅ Pass — payment SDKs only in `payments/adapters/` |
| `test_god_views.py` | ✅ Pass — no view file exceeds 300 lines |
| `test_god_models.py` | ✅ Pass — no model file exceeds 400 lines |
| `test_circular_deps.py` | ✅ Pass — 0 circular dependencies |
| `test_rentsecure_be_boundary.py` | ✅ Pass — 0 imports from `rentsecure_be/` in apps |
| `test_shared_purity.py` | ✅ Pass — `shared/` does not import Django or apps |
| All existing tests | ✅ Pass — no regressions |

## Rollback Plan

**Rollback Trigger:** Architecture tests are too strict and block legitimate development, or `import-linter.ini` misconfiguration causes false positives.

**Rollback Steps:**
1. `git revert` PR-007 merge commit
2. Restore previous `import-linter.ini`
3. Remove `tests/architecture/` directory (or disable tests)
4. Deploy reverted code
5. Adjust architecture test thresholds if needed

**Rollback Time:** 15 minutes
**Data Risk:** None — no data changes.
**Rollback Testing:** N/A — configuration and tests only.

## Definition of Done

- [ ] `import-linter.ini` completely rewritten without `rentsecure_be` as allowed layer
- [ ] All 8 architecture test files created
- [ ] All architecture tests pass on current codebase
- [ ] Architecture tests integrated into CI pipeline
- [ ] `import-linter check` passes with 0 violations
- [ ] All existing tests pass
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] PR approved by Staff Engineer and Architecture Team
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)

---

# PR-008: Webhook Idempotency & Audit Logging

## Goal

Add webhook idempotency keys (via `WebhookEvent` model) to all webhook handlers and enable audit logging for payment operations using `django-simple-history`.

## Files to Create

| File | Purpose |
|------|---------|
| `payments/tests/test_webhook_idempotency.py` | Tests for duplicate webhook handling (created in PR-004, expanded here) |
| `payments/tests/test_audit_logging.py` | Tests for audit logging on payment models |

## Files to Modify

| File | Change |
|------|--------|
| `payments/views/webhooks.py` | Add idempotency key check using `WebhookEvent` model |
| `payments/models.py` | Add `HistoricalRecords` to `OwnerBankDetails` and `WebhookEvent` |
| `properties/models/rent_record_models.py` | Add `HistoricalRecords` to `RentRecord` for `payout_status` changes |
| `core/models.py` | Add `HistoricalRecords` to `OwnerBankDetails` (if not already moved) |

## Files to Delete

None.

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all tests pass including idempotency and audit tests |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — no violations |
| Security | ✅ Pass — Bandit 0 high/medium |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| Webhook idempotency implemented | ✅ Duplicate webhooks return 200 without reprocessing |
| Audit logging enabled | ✅ `HistoricalRecords` on payment models |
| No circular dependencies | ✅ Confirmed |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |

## Expected MyPy Validation

| Check | Expected Result |
|-------|-----------------|
| `mypy .` | ✅ Pass — 0 errors |

## Expected Ruff Validation

| Check | Expected Result |
|-------|-----------------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| Webhook idempotency tests | ✅ Pass — duplicate webhooks return 200 with original response |
| Audit logging tests | ✅ Pass — historical records created on model changes |
| Existing tests | ✅ All pass — no regressions |
| Security tests | ✅ Pass — no sensitive data in audit logs |

## Rollback Plan

**Rollback Trigger:** Idempotency logic causes missed webhooks, or audit logging performance impact is too high.

**Rollback Steps:**
1. `git revert` PR-008 merge commit
2. Remove `WebhookEvent` checks from webhook handlers (revert to PR-004 state)
3. Remove `HistoricalRecords` from models if causing issues
4. Deploy reverted code
5. Run smoke tests

**Rollback Time:** 15 minutes
**Data Risk:** Low — `WebhookEvent` table can be emptied. Historical records remain but are read-only.
**Rollback Testing:** Tested on staging.

## Definition of Done

- [ ] Webhook idempotency implemented in `payments/views/webhooks.py`
- [ ] `WebhookEvent` model has `HistoricalRecords`
- [ ] `OwnerBankDetails` has `HistoricalRecords`
- [ ] `RentRecord.payout_status` changes are logged
- [ ] All idempotency tests pass
- [ ] All audit logging tests pass
- [ ] All existing tests pass
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] Bandit scan passes (0 high/medium)
- [ ] PR approved by Security Lead and Platform Team Lead
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)

---

# PR-009: Migration Rollback Tests + CI Update

## Goal

Add automated migration rollback tests to CI and update the CI pipeline to enforce all Phase 0 gates.

## Files to Create

| File | Purpose |
|------|---------|
| `tests/test_migrations.py` | Automated forward and reverse migration tests |
| `tests/test_migration_data_integrity.py` | Data integrity tests for Phase 0 migrations |

## Files to Modify

| File | Change |
|------|--------|
| `.github/workflows/ci.yml` | Add migration test step, ensure architecture tests run on every commit |
| `pytest.ini` | Add migration test configuration if needed |
| `noxfile.py` | Add migration test session if using nox |

## Files to Delete

None.

## Expected CI Status

| Job | Expected Result |
|-----|-----------------|
| Lint (Ruff) | ✅ Pass — 0 errors |
| Type Check (MyPy) | ✅ Pass — 0 errors |
| Import Rules (import-linter) | ✅ Pass — 0 violations |
| Tests (Pytest) | ✅ Pass — all tests pass including migration tests |
| Django Check | ✅ Pass — `manage.py check` returns 0 |
| Architecture | ✅ Pass — all architecture tests pass |
| Migration Tests | ✅ Pass — forward and reverse migrations work |
| Security | ✅ Pass — Bandit 0 high/medium |

## Expected Architecture Validation

| Gate | Expected Result |
|------|-----------------|
| Migration forward test | ✅ `manage.py migrate` applies all migrations cleanly |
| Migration reverse test | ✅ `manage.py migrate --reverse` reverts all migrations |
| Data integrity test | ✅ No data loss during forward/reverse |
| Architecture tests | ✅ All pass |

## Expected Import-Linter Validation

| Check | Expected Result |
|-------|-----------------|
| `import-linter check` | ✅ Pass — 0 violations |

## Expected MyPy Validation

| Check | Expected Result |
|-------|---------|
| `mypy .` | ✅ Pass — 0 errors |

## Expected Ruff Validation

| Check | Expected Result |
|-------|---------|
| `ruff check .` | ✅ Pass — 0 errors |
| `ruff format --check .` | ✅ Pass — 0 formatting issues |

## Expected Tests

| Test | Expected Result |
|------|-----------------|
| `tests/test_migrations.py` | ✅ Pass — all migrations apply and reverse |
| `tests/test_migration_data_integrity.py` | ✅ Pass — data integrity maintained |
| All existing tests | ✅ Pass — no regressions |
| Architecture tests | ✅ All pass |

## Rollback Plan

**Rollback Trigger:** Migration tests fail, CI pipeline breaks, or false positives in architecture tests.

**Rollback Steps:**
1. `git revert` PR-009 merge commit
2. Revert `.github/workflows/ci.yml` changes
3. Remove `tests/test_migrations.py` and `tests/test_migration_data_integrity.py`
4. Deploy reverted code
5. Verify CI pipeline passes without migration tests

**Rollback Time:** 15 minutes
**Data Risk:** None — no data changes.
**Rollback Testing:** N/A — test code only.

## Definition of Done

- [ ] `tests/test_migrations.py` created with forward and reverse migration tests
- [ ] `tests/test_migration_data_integrity.py` created with data integrity tests
- [ ] `.github/workflows/ci.yml` updated with migration test step
- [ ] Migration tests pass on all branches
- [ ] All existing tests pass
- [ ] `import-linter check` passes
- [ ] `mypy .` passes
- [ ] `ruff check .` passes
- [ ] PR approved by DevOps Engineer and Architecture Team
- [ ] Merged to `phase-0-foundation` branch
- [ ] Deployed to staging
- [ ] Staging validation passed (24 hours)
- [ ] **Phase 0 complete — ready for Phase 1**

---

# Phase 0 Summary

## PR Merge Order

| Order | PR | Title | Dependencies | Risk |
|-------|-----|-------|--------------|------|
| 1 | PR-001 | Foundation — Encrypted Fields & Payments Registration | Phase -1 complete | Low |
| 2 | PR-002 | Migrate OwnerBankDetails to payments | PR-001 | Medium |
| 3 | PR-003 | Migrate NotificationPreference to notification | PR-001 | Medium |
| 4 | PR-004 | Move Webhooks to payments | PR-001 | High |
| 5 | PR-005 | Split core/views.py | PR-001 | Medium |
| 6 | PR-006 | Move Root Management Commands to Apps | PR-001 | Medium |
| 7 | PR-007 | Rewrite import-linter.ini + Add Architecture Regression Tests | PR-001, PR-002, PR-003, PR-004, PR-005, PR-006 | Medium |
| 8 | PR-008 | Webhook Idempotency & Audit Logging | PR-004 | Medium |
| 9 | PR-009 | Migration Rollback Tests + CI Update | All previous PRs | Low |

## CI Gates (All PRs)

| Gate | Tool | Threshold | Blocking |
|------|------|-----------|----------|
| Lint | Ruff | 0 errors | Yes |
| Type Check | MyPy | 0 errors | Yes |
| Import Rules | import-linter | 0 violations | Yes |
| Tests | Pytest | All pass, ≥90% coverage | Yes |
| Django Check | `manage.py check` | 0 errors | Yes |
| Architecture | pytest `tests/architecture/` | 0 failures | Yes |
| Security | Bandit | 0 high/medium | Yes |

## Rollback Summary

| PR | Rollback Method | Data Risk | Rollback Time |
|----|-----------------|-----------|---------------|
| PR-001 | `git revert` + `migrate --reverse` | None | 15 min |
| PR-002 | `git revert` + `migrate --reverse` | Low | 30 min |
| PR-003 | `git revert` + `migrate --reverse` | Low | 30 min |
| PR-004 | `git revert` + restore `core/views.py` | Low | 30 min |
| PR-005 | `git revert` + restore `core/views.py` | None | 15 min |
| PR-006 | `git revert` + restore root commands | None | 30 min |
| PR-007 | `git revert` + restore `import-linter.ini` | None | 15 min |
| PR-008 | `git revert` + remove `WebhookEvent` checks | Low | 15 min |
| PR-009 | `git revert` + remove test files | None | 15 min |

## Phase 0 Exit Criteria

- [ ] All 9 PRs merged to `phase-0-foundation` branch
- [ ] All CI gates passing
- [ ] All architecture tests passing
- [ ] All migration tests passing (forward + reverse)
- [ ] Staging validation passed (all PRs deployed and stable for 48 hours)
- [ ] Security review passed (Bandit 0 high/medium)
- [ ] `payments` registered in `INSTALLED_APPS`
- [ ] `OwnerBankDetails` in `payments/models.py` with encrypted fields
- [ ] `NotificationPreference` in `notification/models.py`
- [ ] Webhooks in `payments/views/webhooks.py` with idempotency
- [ ] `core/views.py` split into 4 modules
- [ ] Root management commands moved to apps
- [ ] `import-linter.ini` rewritten without `rentsecure_be` as allowed layer
- [ ] Architecture regression tests added
- [ ] Migration rollback tests added to CI
- [ ] Phase 0 retrospective completed
- [ ] **Phase 0 sign-off by Staff Engineer**

---

*End of Phase 0 Execution Plan*

**Prepared by:** Principal Software Architect
**Date:** 2026-07-19
**Next Review:** After each PR merge
**Approval Required:** Staff Engineer, Platform Team Lead, Security Lead, DevOps Engineer
